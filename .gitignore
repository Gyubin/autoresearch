# Python
__pycache__/
*.pyc
.venv/

# AutoResearch runtime state — never committed.
# Provenance lives in git branches (hyp/*) + experiments/ledger.jsonl.
.worktrees/
artifacts/
experiments/
insight_memory.json

# Held-out seed: generated by `orchestrator.py init`, deliberately untracked.
# Worktrees materialize tracked files only, so candidate workspaces
# physically lack the held-out configuration.
evaluation/heldout_config.json

# Tooling / local
.orchestrator.lock
.omc/
.claude/
.DS_Store
